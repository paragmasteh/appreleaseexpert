interface:
  display_name: "Mobile Release Expert"
  short_description: "Ship mobile apps with fewer store rejections"
  default_prompt: "Use $mobile-release-expert to audit this React Native or Flutter project, generate release submission artifacts, and triage review rejections."

dependencies:
  tools:
    - type: "mcp"
      value: "context7"
      description: "Context7 MCP server for fresh release and framework docs"
      transport: "streamable_http"
      url: "https://mcp.context7.com/mcp"
